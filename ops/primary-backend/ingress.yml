apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: be-apps-ingress
  namespace: default
  annotations:
    cert-manager.io/cluster-issuer: be-issuer
spec:
  ingressClassName: nginx
  rules:
  - host: api.sitewatch.tech
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: be-service
            port:
              number: 8000
  tls:
  - hosts:
    - api.sitewatch.tech 
    secretName: letsencrypt-prod
    