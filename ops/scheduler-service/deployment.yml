apiVersion: apps/v1
kind: Deployment
metadata:
  name: scheduler-service
  namespace: sitewatch
spec:
  selector:
    matchLabels:
      app: scheduler-svc
  template:
    metadata:
      labels:
        app: scheduler-svc
    spec:
      containers:
        - name: scheduler-svc
          image: bikaxh01/scheduler-srv:7c6fd2b2f824286a53251e41abc11f63d4ad0ba8
          volumeMounts:
          - name: secret
            mountPath: /home/app/.env
            subPath: .env
      volumes:
        - name: secret
          secret: 
           secretName: sitewatch-scheduler-secret
           items: 
            - key: .env
              path: .env 

