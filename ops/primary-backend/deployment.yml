apiVersion: apps/v1
kind: Deployment
metadata:
  name: primary-backend
  namespace: sitewatch
spec:
  selector:
    matchLabels:
      app: primary-be
  template:
    metadata:
      labels:
        app: primary-be
    spec:
      containers:
        - name: primary-be
          image: bikaxh01/sitewatch-backend:b622ab92364f1cdcabf71fdd1b995650c7a978b1
          ports:
            - containerPort: 8000
          volumeMounts:
          - name: secret
            mountPath: /home/app/.env
            subPath: .env
      volumes:
        - name: secret
          secret: 
           secretName: sitewatch-be-secret
           items: 
            - key: .env
              path: .env 
---   
apiVersion: v1
kind: Service
metadata:
  name: be-service
  namespace: sitewatch
spec:
  selector:
    app: primary-be
  ports:
  - port: 8000
    targetPort: 8000
  type: ClusterIP

