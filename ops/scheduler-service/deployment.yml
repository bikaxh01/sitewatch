apiVersion: apps/v1
kind: Deployment
metadata:
  name: scheduler-service
  namespace: sitewatch
spec:
  selector:
    matchLabels:
      app: scheduler-svc
  template:
    metadata:
      labels:
        app: scheduler-svc
    spec:
      containers:
        - name: scheduler-svc
          image: bikaxh01/scheduler-srv:a0d7bb134eef9c95d8ee1d51b9d56080b887fb61
          volumeMounts:
          - name: secret
            mountPath: /home/app/.env
            subPath: .env
      volumes:
        - name: secret
          secret: 
           secretName: sitewatch-scheduler-secret
           items: 
            - key: .env
              path: .env 

