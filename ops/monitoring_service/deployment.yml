apiVersion: apps/v1
kind: Deployment
metadata:
  name: monitoring-service
  namespace: sitewatch
spec:
  selector:
    matchLabels:
      app: monitoring-svc
  template:
    metadata:
      labels:
        app: monitoring-svc
    spec:
      containers:
        - name: monitoring-svc
          image: bikaxh01/monitoring-svc:f46970d8137019242754fc68f22bae5eac026f4c
          volumeMounts:
          - name: secret
            mountPath: /home/app/.env
            subPath: .env
      volumes:
        - name: secret
          secret: 
           secretName: sitewatch-monitoring-secret
           items: 
            - key: .env
              path: .env 

