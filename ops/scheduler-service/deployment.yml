apiVersion: apps/v1
kind: Deployment
metadata:
  name: scheduler-service
  namespace: sitewatch
spec:
  selector:
    matchLabels:
      app: scheduler-svc
  template:
    metadata:
      labels:
        app: scheduler-svc
    spec:
      containers:
        - name: scheduler-svc
          image: bikaxh01/scheduler-srv:89a0e5d0f1c656fb3bc7a511ba2eeed9564e0cd0
          volumeMounts:
          - name: secret
            mountPath: /home/app/.env
            subPath: .env
      volumes:
        - name: secret
          secret: 
           secretName: sitewatch-scheduler-secret
           items: 
            - key: .env
              path: .env 

